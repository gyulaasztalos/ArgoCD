apiVersion: v1
kind: ConfigMap
metadata:
  name: app-grafana-dashboard
  namespace: app
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "/tmp/dashboards/Applications"
data:
  app-grafana-dashboard.json: |
    {

    }
