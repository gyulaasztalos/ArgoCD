apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: transmission-rpc
  namespace: transmission
spec:
  entryPoints:
    - rpc
  routes:
    - match: HostSNI(`*`)
      services:
        - name: transmission-rpc
          port: 9091
