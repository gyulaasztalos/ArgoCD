---
# App secrets from 1Password (vault `kubernetes`, EXISTING item `cake-order-app`):
#   username / password  — iCloud SMTP credentials (app-specific password)
#   rate_hash_salt       — salt for hashed rate-limit keys + the form HMAC
#                          (user adds this third field; generate with
#                           `openssl rand -base64 32`)
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cake-order-app-secret
  namespace: cake-order
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: onepassword-connect
    kind: ClusterSecretStore
  target:
    name: cake-order-app-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      type: Opaque
      data:
        smtp_user: "{{ .smtp_user }}"
        smtp_password: "{{ .smtp_password }}"
        rate_hash_salt: "{{ .rate_hash_salt }}"
        intake_token: "{{ .intake_token }}"
  data:
    - secretKey: smtp_user
      remoteRef:
        key: cake-order-app
        property: username
    - secretKey: smtp_password
      remoteRef:
        key: cake-order-app
        property: password
    - secretKey: rate_hash_salt
      remoteRef:
        key: cake-order-app
        property: rate_hash_salt
    - secretKey: intake_token
      remoteRef:
        key: cake-order-app
        property: intake_token
