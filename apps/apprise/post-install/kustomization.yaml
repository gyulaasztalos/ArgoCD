---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Post-install source: only the observability object the scope boundary keeps out
# of the chart. The `mailrise` IngressRouteTCP now renders from the chart
# (templates/ingressroutetcp.yaml, values `ingressRouteTCP:`) as of common 0.6.0.
#
#   - prometheus-alarms.yaml  the apprise PrometheusRule.
resources:
  - prometheus-alarms.yaml
