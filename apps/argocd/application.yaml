---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argocd
spec:
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  sources:
    - repoURL: https://argoproj.github.io/argo-helm
      targetRevision: HEAD
      helm:
        valueFiles:
          - $config/values.yaml
      path: argo-cd
    - repoURL: 'git@github.com:gyulaasztalos/ArgoCD.git'
      targetRevision: main
      path: apps/argocd
      ref: config
  project: default
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
