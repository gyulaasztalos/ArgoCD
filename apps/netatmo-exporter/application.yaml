---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: netatmo-exporter
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  sources:
    # 1) this repo — provides the tailored valueFile (referenced as $config below)
    - repoURL: 'https://github.com/gyulaasztalos/ArgoCD.git'
      targetRevision: main
      ref: config
    # 2) the homelab-charts monorepo — the netatmo-exporter wrapper chart + the
    #    `common` library dependency (resolved from the same checkout)
    - repoURL: 'https://github.com/gyulaasztalos/homelab-charts.git'
      targetRevision: main
      path: charts/netatmo-exporter
      helm:
        valueFiles:
          - $config/apps/netatmo-exporter/values.yaml
    # 3) auxiliary observability assets kept out of the chart (Grafana dashboard)
    - repoURL: 'https://github.com/gyulaasztalos/ArgoCD.git'
      targetRevision: main
      path: apps/netatmo-exporter/post-install
  destination:
    server: https://kubernetes.default.svc
    namespace: netatmo-exporter
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
